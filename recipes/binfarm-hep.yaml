inherit: [in-docker]

root: true

depends:
    - binfarm-base
    - gentoo-portages

buildVars:
    - DOCKER_CMD
    - GENTOO_STG3_TAG

buildScript:
    docker run --rm --volume 
    #mkdir -p assets
    #echo "FROM gentoo-hep-base:${GENTOO_STG3_TAG}" > Dockerfile
    #cat $<<gentoo/hep-bootstrap-no-ms.Dockerfile>> >> Dockerfile
    #$DOCKER_CMD build \
    #    --build-arg ONE="two" \
    #    .

